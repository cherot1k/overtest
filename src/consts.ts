const fortniteClassId = 7314;

const interestingFeatures = [
  'roster',
  // 'match_info'
];

const windowNames = {
  inGame: 'in_game',
  desktop: 'desktop'
};

const hotkeys = {
  toggle: 'showhide'
};

export {
  fortniteClassId,
  interestingFeatures,
  windowNames,
  hotkeys
}